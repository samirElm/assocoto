<div class="container">
  <h1>Créer un CR</h1>

  <%= simple_form_for [:project, :mission, @report] do |f| %>
    <%= f.input :title %>
    <%= f.input :description %>
    <%= f.input :date %>
    <%= f.button :submit, class: "btn btn-primary" %>
  <% end %>
</div>
